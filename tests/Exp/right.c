//Exp测试
int main() {// 1. 赋值与算术优先级
    int x,y=1,z=2;
    x = y + z * 2;
    // 2. 逻辑与 / 逻辑或
    a && b || c;
    // 3. 结构体访问
    p.field;
    // 4. 数组访问（下标表达式中混合算术）
    arr[idx + 1];
    // 5. 一元减号与括号
    -(a + b);
    // 6. 逻辑非
    !flag;
    // 7. 带参数的函数调用
    foo(1, 2 + 3, bar());
    // 8. 无参数函数调用
    baz();
    // 9. 整型和浮点常量
    42;
    3.14;
    return 0;
}
